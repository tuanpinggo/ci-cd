apiVersion: v1
kind: Service
metadata:
  name: hl-fresh
  labels:
    app.kubernetes.io/name: hl-fresh
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hl-fresh
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
